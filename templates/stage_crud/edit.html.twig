<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modifier un stage - La Chataigneraie</title>
    <link rel="stylesheet" href="{{ asset('CSS/stage.css') }}">
</head>
<body>
    <main class="stage-page">
        <section class="stage-form-card">
            <div class="stage-header">
                <h1>Modifier un stage</h1>
                <p class="stage-subtitle">Mettez a jour les informations du stage.</p>
            </div>

            <div class="stage-form-wrap">
                {{ include('stage_crud/form.html.twig', {'button_label': 'Enregistrer les modifications'}) }}
            </div>

            <div class="stage-actions-row">
                <a href="{{ path('app_stage_read') }}" class="btn-secondary">Retour</a>
                {{ include('stage_crud/delete_form.html.twig') }}
            </div>
        </section>
    </main>
</body>
</html>
