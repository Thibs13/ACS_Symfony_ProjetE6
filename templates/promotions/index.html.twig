<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Promotions - La Chataigneraie</title>
    <link rel="stylesheet" href="{{ asset('CSS/dashboard.css') }}">
    <link rel="stylesheet" href="{{ asset('CSS/etudiant.css') }}">
</head>
<body>

<aside class="sidebar">
    <div class="sidebar-logo">
        <div class="logo-badge">
            <img src="{{ asset('Image/campus-logo.png') }}" alt="Logo Campus La Chataigneraie" />
        </div>
        <h2>La Chataigneraie</h2>
        <p>Suivi des stages</p>
    </div>

    {% include 'navigation.html.twig' with {
                'namePage' : 'promotion' 
                } %}

    <div class="sidebar-footer">
        <a href="{{ path('app_deco') }}" class="btn-deco">
            <svg fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path d="M9 21H5a2 2 0 01-2-2V5a2 2 0 012-2h4"/>
                <polyline points="16 17 21 12 16 7"/>
                <line x1="21" y1="12" x2="9" y2="12"/>
            </svg>
            Deconnexion
        </a>
    </div>
</aside>

<main class="main">
    <div class="page-header-flex">
        <div>
            <h1>Gestion des promotions</h1>
            <p>Consulter, modifier et creer les fiches promotions.</p>
        </div>
        <a href="{{ path('app_promotions_create') }}" class="btn-add">Ajouter une promotion</a>
    </div>

    <div class="section-card table-wrapper">
        <table class="students-table">
            <thead>
                <tr>
                    <th>Classe</th>
                    <th>Session</th>
                    <th>Date de début</th>
                    <th>Date de fin</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            {% for promotion in promotions %}
                <tr>
                    <td>{{ promotion.proLibelle }}</td>
                    <td>{{ promotion.proSession }}</td>
                    <td>{{ promotion.proDatedebut ? promotion.proDatedebut|date('Y-m-d') : '' }}</td>
                    <td>{{ promotion.proDatefin ? promotion.proDatefin|date('Y-m-d') : '' }}</td>
                    <td>
                        <div class="table-actions">
                            <a href="{{ path('app_promotions_show', {'id': promotion.id}) }}" class="action-link show" title="Voir">
                                <svg width="16" height="16" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                                    <path stroke-linecap="round" stroke-linejoin="round" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/>
                                    <path stroke-linecap="round" stroke-linejoin="round" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"/>
                                </svg>
                            </a>
                            <a href="{{ path('app_promotions_update', {'id': promotion.id}) }}"class="action-link edit" title="Modifier">
                                <svg width="16" height="16" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                                    <path stroke-linecap="round" stroke-linejoin="round" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z" />
                                </svg>
                            </a>
                        </div>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="6">Aucune promotion trouvée.</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
</main>

</body>
</html>